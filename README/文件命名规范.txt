* 如果要在文件里定义或实现一个类，则应以类名作为文件名。

* 如果要在文件里声明或定义一个函数，则应以函数名作为文件名。

* 如果要在文件里声明或定义一组函数，则应以最具代表性的函数的名称作为文件名。如果找不到比较有代表性的，就取个有概括性的名称，格式（大小写之类的）应与同目录的大多数文件保持一致。

* 浏览代码时，进到一个目录，如果发现有个文件的名称与目录名相同，我会先看这个文件。所以目录中概述性的文件可与目录同名。

* 因为大部分文件名取的是类名，所以其它情况下文件名应尽量与类名格式一致。